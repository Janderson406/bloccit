<div class="form-group">
    <%= f.label :labels %>
 <!-- use a text field so that a user can submit multiple labels at once -->
    <%= f.text_field :labels,
                     class: 'form-control',
                     placeholder: "Enter labels (separated by commas)",
                     value: labels.map(&:name).join(', ') %>
              <!-- value of txt field: labels is the array of label objects.
              map iterates over each label object and creates a new array where
              each value in the new array is a label name. We then use .join(', ')
              on the new array to create a string of labels. -->       
  </div>
